# Backend
JWT_SECRET=change-me
DATABASE_URL=file:/app/data/railpulse.db
TRAFIKVERKET_API_KEY=your-api-key
PORT=9876
TZ=Europe/Stockholm
FRONTEND_URL=http://localhost:9876

# Cookie settings (set COOKIE_SECURE=false if not using HTTPS)
# COOKIE_SECURE=false
# COOKIE_SAMESITE=lax

# PUID=1000
# PGID=1000
# LOG_LEVEL=info
# DATA_DIR=./data
# NODE_ENV=production

# SMTP (optional)
# SMTP_HOST=smtp-host
# SMTP_PORT=465
# SMTP_USER=your-smtp-username
# SMTP_PASS=your-smtp-password
# SMTP_FROM="RailPulse <no-reply@yourdomain.com>"